---
import type { HTMLAttributes } from 'astro/types';
import { ChevronLeft, ChevronUp } from 'lucide-astro';
import { twMerge } from 'tailwind-merge';
import { tv, type VariantProps } from 'tailwind-variants';
import { variants as buttonVariants } from '../../button.astro';

const variants = tv({
  extend: buttonVariants,
  defaultVariants: { size: 'icon', color: 'outline' },
});

type Props = HTMLAttributes<'button'> & VariantProps<typeof variants>;

const { class: className, size, color, ...props } = Astro.props;
---

<button {...props} aria-label="Previous" class={twMerge(variants({ size, color }), className)}>
  <ChevronLeft class="group-data-[orientation=vertical]:hidden" />
  <ChevronUp class="group-data-[orientation=horizontal]:hidden" />
</button>
