---
import type { HTMLAttributes } from 'astro/types';
import { twMerge } from 'tailwind-merge';
import Separator from '../../separator.astro';

type Props = HTMLAttributes<'div'> & { value: string };

const { class: className, value, ...props } = Astro.props;
---

<div
  {...props}
  role="region"
  data-content={value}
  data-expanded="false"
  class={twMerge(
    'overflow-hidden',
    'data-[expanded="false"]:h-0',
    'data-[expanded="false"]:animate-expand-up',
    'data-[expanded="true"]:animate-expand-down',
    className,
  )}
>
  <div data-content-inner class="pb-6"><slot /></div>
</div>
<Separator />
